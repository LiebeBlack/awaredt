name: Android CI

on:
  push:
    branches: [main, master]
  pull_request:
    paths: ["android/**"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: "8.9"

      - name: Build debug APK + lint
        working-directory: android
        run: gradle --no-daemon :app:assembleDebug :app:lintDebug

      # El APK de release es el que se instala en los moviles y es el UNICO que
      # pasa por R8 (minify + shrinkResources). Si no se compila aqui, un fallo de
      # reglas ProGuard solo aparece al etiquetar una version, ya con usuarios.
      - name: Build release APK (R8 + shrinkResources)
        working-directory: android
        run: gradle --no-daemon :app:assembleRelease

      - name: Upload APK artifact
        uses: actions/upload-artifact@v7
        with:
          name: locator-agent-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
